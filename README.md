### Backing up dotfiles the straightforward way

#### neovim
* files should be in `~/.config/`
#### tmux
* file should be in `~` named `.tmux.conf`
#### helix
* file should be in `~./config/helix/config.toml`
#### wezzterm
* file should be in `~/.wezzterm.lua`
